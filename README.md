# PetVetAI 小程序

PetVetAI 是一个基于微信小程序的 AI 宠物医疗助手应用，提供智能症状分析和健康建议功能。

## 📋 目录

- [项目简介](#项目简介)
- [技术栈](#技术栈)
- [项目架构](#项目架构)
- [目录结构](#目录结构)
- [核心模块](#核心模块)
- [后端服务通信](#后端服务通信)
- [配置说明](#配置说明)
- [开发指南](#开发指南)
- [使用说明](#使用说明)

## 🎯 项目简介

PetVetAI 小程序是一个宠物医疗辅助应用，主要功能包括：

- **AI 智能诊断**：基于宠物症状描述，提供 AI 分析建议
- **健康检查**：检查后端服务可用性
- **模块化设计**：清晰的代码结构，便于维护和扩展

## 🛠 技术栈

- **框架**：微信小程序原生框架
- **语言**：JavaScript (ES6+)
- **样式**：WXSS
- **后端服务**：Spring Boot (pet-vet-ai)

## 🏗 项目架构

### 架构设计原则

1. **模块化设计**：功能模块独立，职责清晰
2. **统一入口**：API 调用统一管理，便于维护
3. **环境隔离**：支持多环境配置，便于部署
4. **错误处理**：统一的错误处理和用户提示

### 架构层次

```
┌─────────────────────────────────────────┐
│           页面层 (Pages)                │
│  ┌──────────┐  ┌──────────┐  ┌──────┐  │
│  │  index   │  │diagnosis │  │ demo │  │
│  └──────────┘  └──────────┘  └──────┘  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         服务层 (Services)                │
│  ┌──────────┐  ┌──────────┐            │
│  │diagnosis│  │  health  │            │
│  └──────────┘  └──────────┘            │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│          API 层 (API)                   │
│  ┌──────────┐  ┌──────────┐            │
│  │diagnosis │  │  health  │            │
│  │   Api    │  │   Api    │            │
│  └──────────┘  └──────────┘            │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│       请求层 (Request)                   │
│  ┌──────────────────────────────┐       │
│  │      request.js              │       │
│  │  (封装 wx.request)          │       │
│  └──────────────────────────────┘       │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│       配置层 (Config)                    │
│  ┌──────────────────────────────┐       │
│  │      api.js                  │       │
│  │  (API 配置、环境管理)        │       │
│  └──────────────────────────────┘       │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│        后端服务 (pet-vet-ai)            │
│  ┌──────────────────────────────┐       │
│  │  Spring Boot Application     │       │
│  │  Port: 48080                 │       │
│  └──────────────────────────────┘       │
└─────────────────────────────────────────┘
```

### 数据流向

1. **用户操作** → 页面层触发事件
2. **页面层** → 调用服务层方法
3. **服务层** → 调用 API 层接口
4. **API 层** → 使用请求层发送 HTTP 请求
5. **请求层** → 读取配置层获取 baseURL
6. **HTTP 请求** → 发送到后端服务
7. **响应数据** → 逐层返回，最终更新页面

## 📁 目录结构

```
petvetai-miniprogram/
├── app.js                 # 小程序入口文件
├── app.json               # 小程序全局配置
├── app.wxss               # 小程序全局样式
├── project.config.json    # 项目配置文件
├── sitemap.json           # 站点地图配置
│
├── config/                # 配置目录
│   └── api.js            # API 配置（环境管理、端点定义）
│
├── utils/                 # 工具函数目录
│   ├── api.js            # 统一 API 服务模块
│   ├── request.js        # 网络请求封装
│   ├── health.js         # 健康检查服务
│   └── diagnosis.js      # 诊断服务
│
├── pages/                 # 页面目录
│   ├── index/            # 首页
│   │   ├── index.js
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   └── index.json
│   │
│   ├── diagnosis/        # 诊断页面
│   │   ├── diagnosis.js
│   │   ├── diagnosis.wxml
│   │   ├── diagnosis.wxss
│   │   └── diagnosis.json
│   │
│   └── demo/             # Demo 页面（通信示例）
│       ├── demo.js
│       ├── demo.wxml
│       ├── demo.wxss
│       └── demo.json
│
├── components/            # 组件目录
├── images/               # 图片资源目录
└── README.md             # 项目说明文档
```

## 🔧 核心模块

### 1. 配置模块 (`config/api.js`)

**职责**：统一管理 API 配置，支持多环境切换

**功能**：
- 环境配置管理（development/test/production）
- API 端点定义
- 请求超时配置
- 自动环境检测

**使用示例**：
```javascript
const apiConfig = require('./config/api')
console.log(apiConfig.baseURL)  // 当前环境的 baseURL
console.log(apiConfig.endpoints.DIAGNOSE)  // /api/pet/diagnose
```

### 2. 请求模块 (`utils/request.js`)

**职责**：封装微信小程序的网络请求 API

**功能**：
- 统一请求封装
- 自动加载提示
- 错误处理
- 支持 GET/POST/PUT/DELETE 方法

**使用示例**：
```javascript
const { get, post } = require('./utils/request')

// GET 请求
const data = await get('/api/endpoint', { param: 'value' })

// POST 请求
const result = await post('/api/endpoint', { data: 'value' })
```

### 3. API 服务模块 (`utils/api.js`)

**职责**：提供所有后端 API 调用的统一入口

**模块**：
- `healthApi`：健康检查相关 API
- `diagnosisApi`：诊断相关 API

**设计优势**：
- 统一管理所有 API 调用
- 便于维护和扩展
- 清晰的模块划分

**使用示例**：
```javascript
const { healthApi, diagnosisApi } = require('./utils/api')

// 健康检查
const health = await healthApi.checkHealth()

// 提交诊断
const result = await diagnosisApi.submitDiagnosis(1, '症状描述')
```

### 4. 服务模块

#### 健康检查服务 (`utils/health.js`)

**职责**：封装健康检查相关业务逻辑

**功能**：
- 检查后端服务可用性
- 统一错误处理
- 返回标准化结果

#### 诊断服务 (`utils/diagnosis.js`)

**职责**：封装诊断相关业务逻辑

**功能**：
- 提交诊断请求
- 参数验证
- 结果处理

### 5. 页面模块

#### 首页 (`pages/index/`)

**功能**：
- 应用介绍
- 功能入口
- 跳转到诊断页面和 Demo 页面

#### 诊断页面 (`pages/diagnosis/`)

**功能**：
- 宠物 ID 输入
- 症状描述输入
- 提交诊断请求
- 显示诊断结果

#### Demo 页面 (`pages/demo/`)

**功能**：
- 展示服务健康检查功能
- 展示诊断功能调用示例
- 演示小程序与后端服务的通信方式

## 🔌 后端服务通信

### 后端服务信息

- **服务名称**：pet-vet-ai
- **默认端口**：48080
- **协议**：HTTP/HTTPS
- **CORS**：已配置跨域支持

### API 端点

#### 1. 健康检查

- **端点**：`GET /actuator/health`
- **说明**：检查后端服务健康状态
- **响应**：服务状态信息

#### 2. 宠物诊断

- **端点**：`POST /api/pet/diagnose`
- **请求体**：
  ```json
  {
    "petId": 1,
    "symptomDesc": "症状描述"
  }
  ```
- **响应**：
  ```json
  {
    "suggestion": "诊断建议",
    "confidence": 0.8
  }
  ```

### 通信流程

```
小程序端                   后端服务
   │                          │
   │  ── HTTP Request ────>   │
   │                          │
   │  <── HTTP Response ────  │
   │                          │
```

### 错误处理

- **网络错误**：显示"网络请求失败"提示
- **HTTP 错误**：根据状态码显示相应错误信息
- **业务错误**：显示后端返回的错误信息

## ⚙️ 配置说明

### API 配置 (`config/api.js`)

#### 环境配置

```javascript
const env = {
  development: {
    baseURL: 'http://localhost:48080',  // 开发环境
    timeout: 30000
  },
  test: {
    baseURL: 'http://test.example.com:48080',  // 测试环境
    timeout: 30000
  },
  production: {
    baseURL: 'https://api.petvetai.com',  // 生产环境
    timeout: 30000
  }
}
```

#### 环境切换

环境会根据以下优先级自动检测：

1. 编译时环境变量 (`process.env.NODE_ENV`)
2. 小程序运行时环境 (`wx.getAccountInfoSync()`)
3. 默认使用开发环境

#### 本地开发配置

**重要提示**：小程序中不能直接使用 `localhost`

**解决方案**：

1. **使用局域网 IP**（推荐）
   ```javascript
   development: {
     baseURL: 'http://192.168.1.100:48080',  // 替换为你的本机 IP
     timeout: 30000
   }
   ```

2. **微信开发者工具设置**
   - 设置 → 项目设置 → 本地设置
   - 勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"

3. **真机调试**
   - 确保手机和电脑在同一网络
   - 使用电脑的局域网 IP 地址

### 项目配置 (`project.config.json`)

主要配置项：

- `urlCheck: false`：不校验合法域名（开发环境）
- `es6: true`：启用 ES6 转 ES5
- `minified: true`：压缩代码

## 🚀 开发指南

### 环境要求

- 微信开发者工具（最新版本）
- Node.js（如需要构建工具）

### 开发步骤

1. **克隆项目**
   ```bash
   git clone https://github.com/dds521/petvetai-miniprogram.git
   cd petvetai-miniprogram
   ```

2. **配置 API 地址**
   - 编辑 `config/api.js`
   - 修改 `development.baseURL` 为你的后端服务地址

3. **启动后端服务**
   - 确保 `pet-vet-ai` 服务运行在 48080 端口

4. **打开项目**
   - 使用微信开发者工具打开项目目录
   - 配置 AppID（测试可使用测试号）

5. **开发调试**
   - 在开发者工具中预览
   - 使用真机调试测试网络请求

### 添加新 API

1. **在 `config/api.js` 中添加端点**
   ```javascript
   endpoints: {
     NEW_API: '/api/new/endpoint'
   }
   ```

2. **在 `utils/api.js` 中添加 API 方法**
   ```javascript
   const newApi = {
     callNewApi(data) {
       return post(apiConfig.endpoints.NEW_API, data)
     }
   }
   
   module.exports = {
     healthApi,
     diagnosisApi,
     newApi  // 导出新 API
   }
   ```

3. **创建服务模块**（可选）
   ```javascript
   // utils/newService.js
   const { newApi } = require('./api')
   
   function callNewService(data) {
     return newApi.callNewApi(data)
   }
   
   module.exports = { callNewService }
   ```

4. **在页面中使用**
   ```javascript
   const { newApi } = require('../../utils/api')
   
   // 或使用服务模块
   const { callNewService } = require('../../utils/newService')
   ```

## 📖 使用说明

### 访问 Demo 页面

1. 从首页点击"查看通信 Demo"按钮
2. 或直接访问 `/pages/demo/demo` 页面

### 功能演示

#### 1. 健康检查

- 点击"检查服务"按钮
- 查看服务状态和响应数据
- 验证后端服务是否可用

#### 2. AI 诊断

- 输入宠物 ID（示例：1）
- 输入症状描述（示例：狗狗咳嗽、食欲不振）
- 点击"提交诊断"按钮
- 查看 AI 诊断结果

### 注意事项

1. **网络配置**：确保小程序能访问后端服务地址
2. **后端服务**：确保后端服务正常运行
3. **CORS 配置**：后端已配置跨域，无需额外设置
4. **环境切换**：根据实际环境修改 API 配置

## 📝 代码规范

### 命名规范

- **文件命名**：小写字母，使用连字符（kebab-case）
- **变量命名**：驼峰命名（camelCase）
- **常量命名**：大写下划线（UPPER_SNAKE_CASE）
- **函数命名**：动词开头，驼峰命名

### 代码组织

- **模块化**：功能独立，职责单一
- **可复用**：公共逻辑提取为工具函数
- **可维护**：代码清晰，注释完善

## 🔗 相关链接

- [后端服务仓库](https://github.com/dds521/pet-vet-ai)
- [微信小程序文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)

## 📄 许可证

本项目采用 MIT 许可证。

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

---

**最后更新**：2025-01-27
